#include<iostream>
#include<cmath>
using namespace std;

int main(){
    float a,b,p;
    cout<<"Enter a base number : ";
    cin>>a;
    cout<<"Enter raise to number : ";
    cin>>b;
    p = pow(a,b);
    cout<<"Base raise to the power is : "<<p<<endl;

}